package _20190906

// f[i,j] = f[i+1, j-1] and si == sj

func longestPalindrome(s string) string {
	length := len(s)
	//var result = make([]int, length*2)
	for i := 0; i < length; i++ {
		for j := 0; j < length; j++ {
		}
	}
	return ""
}
